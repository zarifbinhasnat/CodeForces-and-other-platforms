#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        int n;
        cin>>n;
        long long a[n+1];
        for(int i=1;i<=n;i++)
        {
            cin>>a[i];
        }
        int now = n;
        long long ans  = LLONG_MIN;\
        for (int step = 1;step <= n ;step ++  )
        {
            
        }

    }
}