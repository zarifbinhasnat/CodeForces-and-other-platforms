#include<bits/stdc++.h>
using namespace std;

int main() {
    freopen("input.txt","r",stdin); 
    freopen("output.txt","w",stdout); 
    int t;
    cin>>t;
    while(t--)
    {
        int n,m;
        cin>>n>>m;
        #include <vector> // Include directive for the vector class

                vector<int> v(n);
                for(int i = 0; i < n; i++) {
                    cin>>v[i];
                }
                if(n>m)
                {
                    cout<<"YEs"<<endl;
                }
                else
                {
                    // Add missing closing curly brace and return statement
                }
            }
            return 0;
        }
