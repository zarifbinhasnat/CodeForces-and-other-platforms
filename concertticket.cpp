#include <bits/stdc++.h>
using namespace std;

int main()
{

    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n, m;
    cin >> n >> m;
    vector<int> h(n);
    vector<int> t(m);

    for (int i = 0; i < n; i++)
    {
        cin >> h[i];
    }
    sort(h.begin(),h.end());
    for (int i = 0; i < m; i++)
    {
        cin >> t[i];
    }
    
    for(int i=0;i<m;i++)
    {

    }

    return 0;
}
