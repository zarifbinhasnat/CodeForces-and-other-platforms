#include<bits/stdc++.h>
using namespace std;

int main() {
   
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    srand(time(0));
    int total_points =10000;
    int cnt=0;
    for (int i =0;i<total_points;i++)
    {
        double x= double 
    }


    return 0;
}
